# Acho que ficou bom, avaliem com carinho!

Por favor, para rodar local basta, apenas, dentro da pasta raiz do projeto executar os comandos `dotnet restore` e `dotnet run -p|--project <PATH>`, aplicação executará e ficará acessível na porta **5000** do seu endereço localhost.
